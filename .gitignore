# 归档与二进制产物：源码仓库里不放这些。
# .gitignore 只在文件还没被跟踪时起作用，`git add -f` 能绕过它 —— 真正的护栏是
# scripts/testing/repository-contents.mjs，它的判据是 git ls-files（库里实际有什么）。
*.zip
*.7z
*.tar
*.tar.gz
*.rar
*.exe
*.dll
*.pdb
*.log
*.tmp
*.bak
*.orig

# Rust build output
app/.cargo/
app/target/
app/target-codex/
extension/wasm/visit-graph-engine/target/

# Local debug/output scratch space
docs/
output/
dist/
.claude/
.codex-browser-profiles/
scripts/debug-consent/
extension/shared/local-ai-test-config.js

# OS/editor noise
.DS_Store
Thumbs.db

# 恰好落在本仓库目录里的另一个独立项目（自带 README/PROGRESS/.gitignore，
# 含 Blender 源文件与导出资产，约 64 MB / 161 个文件）。它和智能预加载没有任何关系，
# 不应进入本仓库的历史。文件仍留在磁盘上，建议按 D:\Code\<project-name> 的约定移出去。
/技术演示，逻辑帧和渲染帧分离/
